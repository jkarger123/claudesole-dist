# Airtable extension -- session handoff 20260708-1825

## Accomplished this session
- Sarah (afp operator) supplied her Airtable PAT via the operator-note channel.
- Routed it into AFP's vault the secret-clean way: filed a secure-field REQUEST against AFP
  (`POST http://localhost:8851/api/secure-request`, PIN 3673), Sarah pasted it into the dashboard modal ->
  stored as vault key `AIRTABLE_API_KEY` (scope `afp`). Value never entered chat.
- Installed + wired the airtable extension on AFP (`POST /api/extension-install {id:airtable}`) ->
  `npx airtable-mcp-server` block now in AFP's `.mcp.json` referencing `${AIRTABLE_API_KEY}`.
- Live-verified the token end-to-end against the Airtable API (token handled via 0600 scratch file, shredded).

## Current state (VERIFIED working on AFP)
- Vault `AIRTABLE_API_KEY` = set. Extension installed + MCP wired. node/npx present. Agents on AFP now get
  Airtable tools (list/query/create/update/upsert, 5 req/s/base).
- Token (user usrlOCdSBib6cFuAl) sees 2 bases:
  - `AVENLUR` (`app58zxrnoAKrn92s`) -- permission **READ ONLY**. 11 tables (Content Calendar, Content Ideas,
    Influencers/Email List, Giveaway Winners, Marketing Assets, Other Tasks, 2026 New Items, Products, ...).
  - `Product Database` (`appGihBhfNDheuYrF`) -- permission `create` (read/write).
- `schema.bases:read` + `data.records:read` confirmed live (read real Content Calendar rows).

## Open items / decisions for next agent
1. WRITE not enabled on AVENLUR -- extension "edit-in-place" won't work there until Sarah re-grants the PAT
   with `data.records:write` on that base. Read-only may be the intended safe default -- operator decision.
2. The PAT traveled in plaintext via the operator note -- rotate it eventually so the note-channel copy dies.
3. Incorporation ideas proposed (build at MC, config-driven, ship to fleet -- NOT afp-only hacks), priority:
   - A. Agents: cache base->table->field schema snapshot + inject tiny pointer into agent brief; or a scoped
        "Airtable agent" like the Google agent. (write-back needs scope #1)
   - B. Morning Brief source: today/this-week Content Calendar + stuck-status posts + draft ideas. WORKS read-only.
   - C. Draggable/lens: extension has no `lens:{}` object -> add one for a dashboard widget. WORKS read-only.
   - D. Routines + cross-links (Telegram digest; Content Calendar->Google Calendar, Email List<->Gmail, Assets<->Drive).
   User leaned toward starting with B or C but did not commit -- confirm before building.

## Key pointers
- Extension: this dir (`extension.json`, `mcp.json` template, `AGENT.md` rules, `SETUP.md`, `CLAUDE.md`).
- Vault mechanics: `command-center/vault/CLAUDE.md` (secure-field request flow = sec 5; `cc-secure` CLI).
- AFP node: user sarahaios, localhost:8851 (tailnet peer `afp` :8850), PIN 3673, own vault. See memory `afp-node-ops`.
